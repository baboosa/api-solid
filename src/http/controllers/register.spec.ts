import { it } from 'node:test'
import { describe } from 'vitest'

describe('RegisterController', () => {
  it.skip('should register a user successfully', async () => {
    console.log('User registration test executed')
  })
})
